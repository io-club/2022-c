#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[]) {
  int x = 1;
  int y = 2;
  int z = f(x, y);
  printf("%d\n", z);
  return 0;
}

int add(int x, int y) {
  int res = strcmp("aaa", "bbb");
  x + y;
}